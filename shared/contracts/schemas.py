"""Shared API schemas and models (placeholder).

In future, move Pydantic models and jsonschema definitions here so both
API and UI import a single source of truth.
"""

# Placeholder until models are extracted from routers.
