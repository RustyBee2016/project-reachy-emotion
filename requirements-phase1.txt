# Phase 1 Dependencies
# Install with: pip install -r requirements-phase1.txt

# API Client dependencies
aiohttp>=3.8.0
requests>=2.28.0
# Required on Ubuntu 1 media API host for /api/v1/ingest/prepare-run-frames (cv2)
opencv-python-headless>=4.8.0,<5

# WebSocket dependencies
python-socketio[asyncio]>=5.7.0

# Database dependencies
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
psycopg[binary]>=3.1,<4

# Testing dependencies
pytest>=7.3.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# updates per Codex instructions
httpx>=0.27.0
sqlalchemy[asyncio]>=2.0.0  # replaces bare SQLAlchemy line
aiosqlite>=0.19.0
