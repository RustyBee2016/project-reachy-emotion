**2025-11-03-Step 6.B-Progress Report_Integrations - Dry-Runs of
Postgres & File system + Manifest Rebuild**

**Designing end-to-end test environment**

This plan sets up detailed HTTPX end-to-end tests with a temporary
SQLite database, isolated filesystem paths, and a stub manifest backend.
I\'ll override FastAPI dependencies to inject test settings, database
sessions, and backend, run Alembic migrations for schema setup, and
carefully manage resource cleanup between tests. I\'ll create tests for
real and dry-run promote flows verifying DB updates, file moves, and
backend scheduling while ensuring each test runs in a clean isolated
environment. This approach balances realism with test speed and
reliability.

\[MEMORY BANK: ACTIVE\]

Progress:

• Service-layer tests now inject a stub manifest backend, confirming
schedule

calls where appropriate and eliminating reliance on logging defaults

\@tests/apps/api/services/test_promote_service.py#12-552.

• Added async end-to-end coverage for the promotion router using
dependency

overrides: stage/sample real vs. dry-run flows are exercised against a
temporary

SQLite DB and filesystem, with manifest backend calls observed

\@tests/apps/api/e2e/test_promote_end_to_end.py#1-316.

Outstanding (per prior plan):

1\. Add manifest reset entry point and tests.

2\. Include async E2E coverage for that reset once implemented.

Next steps:

1\. Extend router/service with a reset route that drives
PromoteService.reset_manifest.

2\. Update schemas if needed and back with unit + E2E assertions (reuse
current

manifest stub).

Let me know when to proceed on the reset hook or if any adjustments are
needed before then.

\[MEMORY BANK: ACTIVE\]

Summary:

• Added manifest reset request/response schemas with validation and
exports to the

shared schema module.@apps/api/app/schemas/promote.py#142-182

\@apps/api/app/schemas/**init**.py#3-19

• Exposed /promote/reset-manifest in the router, wiring it through

PromoteService.reset_manifest and returning correlation-aware

responses.@apps/api/app/routers/promote.py#10-108

• Extended unit tests and new async E2E suite to assert manifest reset
delegation

and API behavior using the manifest stub

backend.@tests/apps/api/services/test_promote_service.py#441-446

\@tests/apps/api/e2e/test_promote_end_to_end.py#318-335

Tests: Not run (not requested).

\[MEMORY BANK: ACTIVE\]

Ran targeted pytest suite covering the updated service and e2e tests:

pytest tests/apps/api/services/test_promote_service.py
tests/apps/api/e2e/test_promote_end_to_end.py

All 16 tests passed (16 passed in 5.29s).

Step 6 is complete.
